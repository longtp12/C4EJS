// Q3
    // a)
    let x1 = parseFloat(prompt("Nhập giá trị x1"));
    let y1 = 4* (x1**2 + 10*Math.sqrt(x1+3*x1+1));
    console.log(y1);
// b)
    let x2 = parseFloat(prompt("Nhập giá trị x2"));
    let y2 = (Math.sin(Math.PI * x2**2) + Math.sqrt(x2**2 +1))/(Math.E**(2*x2) + Math.cos(x2 * Math.PI/4));
    console.log(y2);
    

